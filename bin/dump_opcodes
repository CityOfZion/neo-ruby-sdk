#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'neo/sdk'

contract = Neo::SDK::Contract.load ARGV[0]

puts
puts(contract.script.dump.map { |line| line.join(': ') })
puts
puts contract.script.hash
