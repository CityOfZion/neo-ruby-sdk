#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'neo/sdk'

script = Neo::SDK::Script.load ARGV[0]

puts
puts(script.dump.map { |line| line.join(': ') })
puts
puts script.hash
